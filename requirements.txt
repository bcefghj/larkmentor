# ── Core ──
lark-oapi>=1.5.0
openai>=1.0.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
apscheduler>=3.10.0
fastapi>=0.110.0
uvicorn>=0.27.0
rank-bm25>=0.2.2
numpy>=1.24.0

# ── Testing ──
pytest>=7.0.0
pytest-cov>=4.1.0
pytest-timeout>=2.2.0

# ── Multi-end sync ──
websockets>=12.0
y-py>=0.6.2; python_version < "3.13"

# ── Agent harness ──
python-multipart>=0.0.9
requests>=2.32.0
structlog>=24.1.0

# ── Long-term memory (optional, falls back to local JSONL) ──
mem0ai>=0.1.30; python_version >= "3.10"

# ── Observability ──
prometheus-client>=0.20.0
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
opentelemetry-instrumentation-fastapi>=0.46b0
